# Human-Agent Interaction

## Definition

Human-agent interaction is the control relationship between a user and an agent system during planning, execution, interruption, review, and correction.

## Architectural Notes

- The interface defines what the user can inspect and steer.
- Approval gates encode trust boundaries.
- Good interaction design exposes uncertainty and progress without requiring the user to watch every token.
